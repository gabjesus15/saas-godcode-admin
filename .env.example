# Copia a .env / .env.local y rellena valores reales.
# Documentación de variables completas: ver despliegue interno o proveedores.

# --- Telegram (alertas solo panel super admin) ---
# Token desde @BotFather. Nunca commitear valores reales.
TELEGRAM_BOT_TOKEN=
# Opcional si ya conoces tu chat_id (varios separados por coma). Si no, ejecuta: npm run telegram:pair
TELEGRAM_CHAT_ID=
